from copy import deepcopy

def get_context(context):
    # You can update context here if needed
    # context.update({
    #     "user_name": "stonezhong",
    #     "user_id": 1000,
    #     "group_name": "stonezhong",
    #     "group_id": 1000
    # })
    return context
